<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.css" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body class="container">
    <header>
      <h1><%= title %></h1>
    </header>

    <div role="main">
        <table class="table">
            <% for (var obj of content) { %>
            <tr>
                <td><%= obj.id %></td>
                <td><%= obj.question %></td>
                <td><a href="/manage/edit/<%= obj.id %>">編集</a></td>
                <td>
                    <form method="post" action="/manage/delete/<%= obj.id %>">
                        <input type="submit" value="削除" />
                    </form>
                </td>
            </tr>
            <% } %>
        </table>
        <a href="/manage/register">登録</a>
    </div>
  </body>
</html>
